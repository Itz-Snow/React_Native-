import * as React from 'react';
import { Text, View } from 'react-native';

export default function LittleLemonHeader() {
    return(
        <View >
            <Text style={{fontSize:20}} > Little Lemon </Text>
        </View>
    );
}